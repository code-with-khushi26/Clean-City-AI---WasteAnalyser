# Netlify configuration for React Router
[build]
  publish = "build"
  command = "npm run build"

# Redirect all requests to index.html for client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
```

3. **Save** the file

**Option B: Create _redirects file**

1. Go to your `public/` folder
2. Create a new file called `_redirects` (no extension)
3. Paste this single line:
```
/*    /index.html   200